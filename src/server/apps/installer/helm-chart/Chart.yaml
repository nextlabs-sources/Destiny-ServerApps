apiVersion: v2
name: nextlabs-control-center-installer
description: NextLabs Control Center Intaller Service Helm Chart for Kubernetes
type: application
version: "${appVersion}"
appVersion: "${appVersion}"
